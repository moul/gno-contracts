# CLAUDE.md

**[AGENTS.md](./AGENTS.md) is this repository's guide. Read it before writing code**: it
carries the reasoning, the worked examples, and every trap that has cost a red CI. This
page is its index, one line per rule, for the ones whose violation is silent or
irreversible.

- **The version is the `module` line in `gnomod.toml`; the directory carries none.** A
  breaking change (removing or renaming an exported symbol, changing a signature or an
  on-chain behavior, swapping the backing storage) is `gnopm bump <name>`, then edit the
  files in place so git diffs the change. **Never create a `vN` directory, never copy a
  package to bump it.** New contracts start at `v0`; non-breaking work (new functions,
  tests, comments, docs) edits the current version.
- **`r/moul/home` carries no version at all**, because gnoweb builds `/r/<user>/home` by
  concatenation for `gno.land/u/<user>` and resolves no version. Do not generalise it.
- **`gnomod.lock` is source and a pull request carries it.** Unlike `contracts.json` it is
  not regenerated on `main`: a bump has to ship the pin that keeps the outgoing version
  resolvable, or CI cannot build what still imports it. `gnopm sync` after adding or
  removing a package, `gnopm status` to see where things stand. A superseded version has
  no directory; `sync` rebuilds it into the gitignored `.gnopm/`, and it is still linted
  and tested from there.
- **The 12 mirrored `p/moul/*` are frozen byte-for-byte** with `gnolang/gno`: `addrset`
  `authz` `dynreplacer` `fifo` `helplink` `md` `mdtable` `once` `realmpath` `txlink`
  `typeutil` `ulist`. Never hand-edit one, not a test, not a comment, or `make sync` stops
  meaning anything. Changes go in a `v1`. `README.md` is the one file we add.
- **A pull request carries only source**, plus `gnomod.lock`. `contracts.json`, the README
  table, README footers and `_assets/` are written on `main` by the `main` workflow, and
  CI's `guard-generated` rejects a branch that carries one. Never `make gen` in a branch.
- **Keep the repo autonomous.** Local `p/moul` and `r/moul` imports resolve via
  `gnowork.toml`; every external `gno.land/*` dep is vendored under `vendor/`
  (`make deps`). Only the stdlibs come from `$GNOROOT`.
- **Reusable logic ships as a pure `p/` library plus a thin `r/…demo` realm** that imports
  it and shows it through `Render`, the two cross-referencing each other in the doc
  comment and the README. A lone realm only when it is a stateful app with nothing to
  extract.
- **Every realm tests its `Render`**, preferably an in-package `ExampleRender` doing
  `print(Render(…))` with an `// Output:` block. Without that block the example is
  silently skipped, and only a master gno validates examples at all (`make toolcheck` is
  the canary). Output with two consecutive blank lines can never be pinned by an example:
  assert it with `uassert.Equal` in a normal `Test`.
- **A README must say something, or not exist.** Never a placeholder: `make guard-readmes`
  fails on TODO / TBD / WIP and on a body under 20 characters. Hand-written text goes
  above the footer marker; the footer is generated by `make readmes`.
- **A contract driven from a laptop ships a Go companion at `tools/<name>/`**, in the
  `tool` block of `tools/go.mod`, standard library only, and it **prints `gnokey maketx`
  commands rather than signing anything**. It cannot live beside the contract: the root
  holds `vendor/gno.land`, so it can never be a Go module and nothing would build it.
- **gno is not Go** in six ways that compile in your head and fail in CI. AGENTS.md
  § *Where gno differs from Go* names them, and takes the next one.
- **Green before commit:** `make lint test`, with `GNOROOT` on a gnolang/gno checkout.

## Commits

Conventional, single line: `feat(<name>): …`, `fix: …`, `chore(deps): …`. Author
`Manfred Touron <94029+moul@users.noreply.github.com>`. **Never** a Claude or AI
co-author trailer. Never a compiled binary: the CLIs run via `go -C tools tool <name>`.

## Handy

```sh
make help                   # every target, grouped
make lint test              # the gate
make test PKG=x/daily/b58   # one package
make guard-readmes LIST=1   # which packages are undocumented
make sync                   # drift vs the gnolang/gno monorepo
make publish PRINT=1        # what the chain is missing, as a plan you review
```
