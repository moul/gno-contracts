# `gno.land/r/moul/x/upgrade/store/logic/v1`

Version 1 of the **logic realm** of pattern C. Same shape as [`v0`](../v0),
different rule (step 1000), same data. The upgrade changed the code without
touching a byte of state.

See [the pattern](../../README.md) and [the exploration](../../../README.md).
