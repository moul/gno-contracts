# `gno.land/r/moul/x/upgrade/store/root/v0`

The **data realm** of pattern C. Holds the counter and grants write access to
exactly one logic realm at a time, identified by the package path read off the
crossing frame. `SetLive(path)` is the upgrade.

See [the pattern](../../README.md) and [the exploration](../../../README.md).
