# `gno.land/r/moul/x/upgrade/wrap/v0`

Version 0 of the **wrapping versions** upgrade pattern (pattern A). A plain
counter that knows nothing about any successor and stays writable forever.

See [the pattern](../README.md) and [the exploration](../../README.md).
